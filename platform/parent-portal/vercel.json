{
  "buildCommand": "cd ui && npm install && cd ../parent-portal && npm install && npm run build",
  "outputDirectory": "parent-portal/dist",
  "installCommand": "npm install || true",
  "framework": "vite"
}
