# Copy to .env and update values for your local environment.
# Use 'localhost' when running Prisma commands from your host machine (outside Docker).
# Use 'postgres' (the Docker service name) when connecting from inside a container.
DATABASE_URL="postgresql://postgres:localdev@localhost:5432/platform"
