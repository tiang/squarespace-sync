{
  "name": "platform-api",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js",
    "test": "jest",
    "db:seed:prod": "vercel env pull .env.production --environment production -y && node -e \"require('dotenv').config({path:'.env.production'}); require('./prisma/seed');\" ; rm -f .env.production"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^13.6.1"
  },
  "devDependencies": {
    "dotenv": "^16.0.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "prisma": "^5.22.0",
    "supertest": "^7.0.0"
  }
}
